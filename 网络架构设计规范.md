# MDEN 双端网络架构设计规范

> **本文档是 MDEN 项目的网络层基础架构规范。**
> **所有客户端和服务端的网络相关代码，必须严格遵循本文档中的设计。**
> **在编写任何网络功能代码之前，请先完整阅读本文档。**

---

## 一、总览

本文档定义了 MuseDash 联机模组 (MDEN) 的客户端与服务端统一网络架构。

### 核心设计原则

1. **稳定性优先**：使用 TCP 长连接 + 应用层心跳保活，确保不丢包、不漏消息
2. **可扩展性**：通过 OpCode 路由 + Handler 模式，新增功能只需添加新的消息类型和 Handler，无需修改框架代码
3. **性能兼顾**：在满足前两点的基础上，优化序列化效率和网络带宽占用

### 项目路径

| 项目 | 路径 | 说明 |
|------|------|------|
| 共享协议库 | `D:\MuseDashStuff\Mod_SourceCode\MDEN\MDEN.Protocol\` | 定义所有消息结构和 OpCode，客户端与服务端共同引用 |
| 客户端 Mod | `D:\MuseDashStuff\Mod_SourceCode\MDEN\` | MelonLoader Mod，运行在玩家电脑上 |
| 服务端 | `D:\MuseDashStuff\MDEN.Server\` | .NET 8 控制台服务，运行在云服务器上 |

### 项目引用关系

```
  MDEN.Server ──引用──→ MDEN.Protocol ←──引用── MDEN (客户端Mod)
```

客户端和服务端**必须引用同一个 `MDEN.Protocol` 类库**，以此确保双端的消息结构始终一致。任何消息字段的修改只需改一处，双端同步生效。

> 当前双端已经按此路径关系搭建。后续如果复制协议库或文档副本，必须保持与客户端仓库中的 `MDEN.Protocol` 同步，禁止双端各自维护一份不一致的消息定义。

---

## 二、网络传输层

### 2.1 传输协议：TCP

选择 TCP 而非 UDP 的理由：

- 联机打歌的核心数据（分数、准确率、存活状态）必须**可靠传输**，丢包不可接受
- 对战数据的同步频率约 200ms~2000ms（可配置），对实时性要求不极端
- TCP 天然保证顺序和完整性，省去应用层重传和排序逻辑
- 聊天消息、房间状态等控制数据对可靠性要求极高

### 2.2 数据帧格式 (Wire Format)

所有数据通过以下帧格式在 TCP 流上传输：

```
┌──────────────────────────────────────────────────────┐
│  4 字节 (int32, Little-Endian)  │  N 字节 (Payload)  │
│         数据长度 N              │  UTF-8 JSON 字符串  │
└──────────────────────────────────────────────────────┘
```

- **长度头**：4 字节有符号整数 (`int`)，表示后续载荷的字节数，使用 Little-Endian 编码
- **载荷上限**：1 MB (1,048,576 字节)。超过此大小的帧，接收方必须立即断开连接
- **载荷内容**：UTF-8 编码的 JSON 字符串
- **字节序**：所有多字节数值均使用 Little-Endian
- **非法长度**：长度 `<= 0`、超过上限、包体提前结束都视为协议错误。接收方记录日志后关闭该连接，不继续尝试读后续数据
- **发送互斥**：同一个 TCP 连接上的写入必须串行化。客户端和服务端都必须使用 `SemaphoreSlim(1,1)` 或等价机制保护 `NetworkStream.WriteAsync`

### 2.3 序列化：JSON (`System.Text.Json`)

使用 .NET 内置的 `System.Text.Json` 进行序列化和反序列化。

**选择理由**：
- MelonLoader (Il2Cpp) 环境下兼容性最好，无需引入额外的第三方 NuGet 包
- 调试时可以直接阅读网络包内容，排查问题效率高
- 对于本项目的数据量级（房间最多10人，同步频率200ms+），JSON 的性能完全够用
- 客户端和服务端都是 C#，`System.Text.Json` 双端可用

**序列化约定**：
- 属性名使用 **PascalCase**（如 `Op`、`ReqId`、`Payload`），与 C# 属性命名一致
- 枚举序列化为**数值**（`byte`），而非字符串
- `null` 字段在序列化时**省略**（不发送），以节省带宽
- 对战数据等高频消息，其 Payload 字段仍然使用 JSON 对象（不再手工拼二进制）
- 双端使用统一的 `JsonSerializerOptions`。除非协议版本升级，禁止单侧启用不同的命名策略、枚举转换策略或大小写兼容策略
- 反序列化失败属于该消息无效。服务端不得让异常穿透导致进程退出；客户端不得让异常穿透到 Unity 主线程

---

## 三、消息协议层

### 3.1 消息分类

所有网络消息分为三类：

| 类型 | 方向 | 是否需要响应 | 说明 | 示例 |
|------|------|-------------|------|------|
| **Request** | 客户端 → 服务端 | 是（返回 Response） | 请求-响应模式 | 登录、创建房间、加入房间 |
| **Notify** | 客户端 → 服务端 | 否 | 即发即忘 | 聊天消息、对战数据上报 |
| **Push** | 服务端 → 客户端 | 否 | 服务端主动推送 | 房间状态同步、聊天广播 |

### 3.2 消息信封 (Envelope)

#### 客户端 → 服务端

```json
{
    "Op": 256,              // ushort: OpCode 操作码
    "ReqId": 12345,         // uint?: 请求ID，仅 Request 类型有值，Notify 为 null/省略
    "Payload": { ... }      // object: 具体消息体，结构由 OpCode 决定
}
```

#### 服务端 → 客户端

```json
{
    "Op": 257,              // ushort: OpCode 操作码
    "ReqId": 12345,         // uint?: 对应请求的ID，仅 Response 有值，Push 为 null/省略
    "Success": true,        // bool: 仅 Response 有意义
    "Payload": { ... }      // object: 具体消息体
}
```

**关键说明**：
- `ReqId` 由客户端生成（自增 uint），用于将 Response 与 Request 配对
- 当 `ReqId` 为 `null` 或省略时，表示这是一条 Notify（客户端发）或 Push（服务端发）
- 服务端发送 Push 时，`Success` 字段固定为 `true`，可省略
- Request 必须带 `ReqId`。如果客户端以 Request OpCode 发送但没有 `ReqId`，服务端应按协议错误记录并拒绝处理，避免业务调用方永久等待
- Response 必须使用对应 Request 的下一个 OpCode，并带回相同 `ReqId`。例如 `CreateLobbyReq(0x0300)` 的响应必须是 `CreateLobbyResp(0x0301)`
- Notify 和 Push 不允许带 `ReqId`。需要结果的操作必须设计为 Request/Response，不能用 Notify 后再靠 Push 猜测结果
- `Success=false` 时，Payload 至少包含 `Reason` 字段；客户端 Manager 层负责将 Reason 转换为玩家可读提示

### 3.3 OpCode 定义

OpCode 使用 `ushort` 类型，按功能模块划分区间。新增功能时，在对应区间内追加即可。

```csharp
public static class OpCodes
{
    // ============================================
    //  系统 (0x0001 ~ 0x00FF)
    // ============================================
    public const ushort Ping              = 0x0001;  // 客户端 → 服务端
    public const ushort Pong              = 0x0002;  // 服务端 → 客户端
    public const ushort Disconnect        = 0x0003;  // 服务端 → 客户端 (被踢/服务器关闭)

    // ============================================
    //  认证 (0x0100 ~ 0x01FF)
    // ============================================
    public const ushort LoginReq          = 0x0100;
    public const ushort LoginResp         = 0x0101;

    // ============================================
    //  玩家 (0x0200 ~ 0x02FF)
    // ============================================
    public const ushort UpdatePlayerReq   = 0x0200;
    public const ushort UpdatePlayerResp  = 0x0201;
    public const ushort GetPlayerReq      = 0x0202;
    public const ushort GetPlayerResp     = 0x0203;

    // ============================================
    //  房间 (0x0300 ~ 0x03FF)
    // ============================================
    public const ushort CreateLobbyReq    = 0x0300;
    public const ushort CreateLobbyResp   = 0x0301;
    public const ushort JoinLobbyReq      = 0x0302;
    public const ushort JoinLobbyResp     = 0x0303;
    public const ushort LeaveLobbyReq     = 0x0304;
    public const ushort LeaveLobbyResp    = 0x0305;
    public const ushort GetLobbiesReq     = 0x0306;
    public const ushort GetLobbiesResp    = 0x0307;
    public const ushort LobbyReadyReq     = 0x0308;
    public const ushort LobbyReadyResp    = 0x0309;
    public const ushort LobbyKickReq      = 0x030A;
    public const ushort LobbyKickResp     = 0x030B;
    public const ushort LobbyLockReq      = 0x030C;
    public const ushort LobbyLockResp     = 0x030D;
    public const ushort LobbyStopReq      = 0x030E;
    public const ushort LobbyStopResp     = 0x030F;

    // ============================================
    //  播放列表 (0x0400 ~ 0x04FF)
    // ============================================
    public const ushort PlaylistAddReq       = 0x0400;
    public const ushort PlaylistAddResp      = 0x0401;
    public const ushort PlaylistRemoveReq    = 0x0402;
    public const ushort PlaylistRemoveResp   = 0x0403;
    public const ushort PlaylistContinueReq  = 0x0404;
    public const ushort PlaylistContinueResp = 0x0405;

    // ============================================
    //  对战 (0x0500 ~ 0x05FF)
    // ============================================
    public const ushort BattleDataNotify  = 0x0500;  // 客户端上报对战数据 (Notify)
    public const ushort BattleDataPush    = 0x0501;  // 服务端广播对战数据 (Push)
    public const ushort BattleReturnedReq = 0x0502;
    public const ushort BattleReturnedResp= 0x0503;

    // ============================================
    //  聊天 (0x0600 ~ 0x06FF)
    // ============================================
    public const ushort ChatNotify        = 0x0600;  // 客户端发送聊天 (Notify)
    public const ushort ChatPush          = 0x0601;  // 服务端广播聊天 (Push)

    // ============================================
    //  社交 (0x0700 ~ 0x07FF)
    // ============================================
    public const ushort FriendRequestReq  = 0x0700;
    public const ushort FriendRequestResp = 0x0701;
    public const ushort GetFriendsReq     = 0x0702;
    public const ushort GetFriendsResp    = 0x0703;
    public const ushort FriendNotifyPush  = 0x0704;  // 服务端推送好友通知

    // ============================================
    //  房间状态推送 (0x0800 ~ 0x08FF)
    // ============================================
    public const ushort LobbySyncPush        = 0x0800;  // 服务端推送房间完整状态
    public const ushort LobbyAllReturnedPush = 0x0801;  // 所有人都返回房间
    public const ushort LobbyKickedPush      = 0x0802;  // 通知被踢的玩家
}
```

**扩展规则**：
- 新增功能模块时，在下一个 `0xNN00` 区间开始
- Request 和 Response 使用相邻的奇偶码（如 `0x0300` 和 `0x0301`）
- Notify 和 Push 也使用相邻码
- 每个区间最多 256 个操作码，足够扩展

---

## 四、消息体定义 (Payload)

以下是所有消息的 Payload JSON 结构定义。在 `MDEN.Protocol` 类库中，每个消息对应一个 C# 类。

### 4.1 系统消息

#### Ping (0x0001)
```json
// Payload: 无 (null 或 {})
```

#### Pong (0x0002)
```json
// Payload: 无 (null 或 {})
```

#### Disconnect (0x0003)
```json
{
    "Reason": "kicked"  // string: 断开原因
}
```

### 4.2 认证

#### LoginReq (0x0100)
```json
{
    "Uid": "abc123",          // string: 玩家唯一标识 (来自游戏的 PeroUid)
    "Name": "PlayerName",     // string: 玩家昵称
    "IsReconnect": false      // bool: 是否为断线重连
}
```

#### LoginResp (0x0101)
```json
{
    "Version": "1.0.0",       // string: 服务端版本号
    "Token": "xxx-yyy-zzz"    // string: 会话令牌，后续请求无需携带（TCP长连接，会话绑定在连接上）
}
```

### 4.3 玩家

#### UpdatePlayerReq (0x0200)
```json
{
    "Name": "NewName",        // string?: 更新的昵称
    "Bio": "Hello",           // string?: 个人简介
    "ChatColor": "ff00ff",    // string?: 聊天颜色
    "AvatarName": "head_1",   // string?: 头像名称
    "Level": 50,              // int?: 等级
    "GirlIndex": 0,           // int?: 角色索引
    "ElfinIndex": -1,         // int?: 精灵索引
    "FavGirlIndex": 2,        // int?: 最喜欢的角色
    "FavElfinIndex": 1,       // int?: 最喜欢的精灵
    "Customs": ["md5_1"],     // string[]?: 本地已有的自制谱面 MD5 列表
    "Hiddens": ["uid_1"]      // string[]?: 本地隐藏的谱面 UID 列表
}
```
> 所有字段均为可选。客户端只发送需要更新的字段，服务端进行**合并更新**（不覆盖未传的字段）。

#### UpdatePlayerResp (0x0201)
```json
{}  // 空对象表示成功
```

#### GetPlayerReq (0x0202)
```json
{
    "TargetUid": "abc123"     // string: 要查询的玩家 UID
}
```

#### GetPlayerResp (0x0203)
```json
{
    "Uid": "abc123",
    "Name": "PlayerName",
    "Status": 1,              // byte: 0=Offline, 1=Online, 2=InLobby, 3=InBattle
    "Bio": "Hello",
    "ChatColor": "ffffff",
    "AvatarName": "head_0",
    "Level": 50,
    "ELO": 1500,
    "Banned": false,
    "GirlIndex": 0,
    "ElfinIndex": -1,
    "FavGirlIndex": 2,
    "FavElfinIndex": 1,
    "PingMS": 45,
    "Friends": ["uid1", "uid2"],
    "FriendRequests": ["uid3"],
    "Achievements": { "1700000000": 1 }  // key=Unix时间戳, value=成就ID
}
```

### 4.4 房间

#### CreateLobbyReq (0x0300)
```json
{
    "Name": "My Lobby",       // string: 房间名称
    "MaxPlayers": 8,          // ushort: 最大人数 (2~10)
    "PlayType": 0,            // byte: 0=All, 1=VanillaOnly, 2=CustomOnly
    "ChartSelection": 0,      // byte: 0=HostPlaylist, 1=Playlist, 2=Random
    "Goal": 0,                // byte: 0=Accuracy, 1=Score, 2=Custom
    "PlaylistSize": 20,       // ushort: 播放列表容量 (2~32)
    "Password": null           // string?: 密码，null 表示公开房间
}
```

#### CreateLobbyResp (0x0301)
```json
{
    "LobbyId": 42             // int: 新创建的房间 ID
}
```

#### JoinLobbyReq (0x0302)
```json
{
    "LobbyId": 42,            // int: 要加入的房间 ID
    "Password": "1234"        // string?: 私密房间需要提供密码
}
```

#### JoinLobbyResp (0x0303)
```json
{
    "LobbyId": 42,
    "Reason": null             // string?: 失败时的原因，如 "密码错误"、"房间已满"
}
```
> `Success` 字段在外层信封中。`Reason` 仅在 `Success=false` 时有值。

#### LeaveLobbyReq (0x0304)
```json
{}  // 无需额外参数，服务端通过会话知道是谁
```

#### LeaveLobbyResp (0x0305)
```json
{}
```

#### GetLobbiesReq (0x0306)
```json
{}  // 获取所有公开房间列表
```

#### GetLobbiesResp (0x0307)
```json
{
    "Lobbies": [
        {
            "Id": 42,
            "Name": "My Lobby",
            "HostUid": "abc123",
            "HostName": "HostPlayer",
            "PlayType": 0,
            "ChartSelection": 0,
            "Goal": 0,
            "MaxPlayers": 8,
            "PlayerCount": 3,
            "IsPlaying": false,
            "Locked": false
        }
    ]
}
```

#### LobbyReadyReq (0x0308)
```json
{
    "Ready": true             // bool: 准备 / 取消准备
}
```

#### LobbyReadyResp (0x0309)
```json
{}
```

#### LobbyKickReq (0x030A)
```json
{
    "TargetUid": "abc123"     // string: 要踢出的玩家 UID
}
```

#### LobbyKickResp (0x030B)
```json
{}
```

#### LobbyLockReq (0x030C)
```json
{
    "Locked": true            // bool: 锁定/解锁房间（锁定后不能改播放列表，准备开始游戏）
}
```

#### LobbyLockResp (0x030D)
```json
{
    "Result": 0               // int: 0=成功, 5=有人缺少谱面
}
```

#### LobbyStopReq (0x030E)
```json
{}
```

#### LobbyStopResp (0x030F)
```json
{}
```

### 4.5 播放列表

#### PlaylistAddReq (0x0400)
```json
{
    "Entry": "md5#difficulty#ownerName#chartName"   // string: 谱面条目字符串
}
```

#### PlaylistAddResp (0x0401)
```json
{
    "Result": 0               // int: 0=成功, 2=有人隐藏了此谱面, 3=有人没有此自制谱面
}
```

#### PlaylistRemoveReq (0x0402)
```json
{
    "Entry": "md5#difficulty#ownerName#chartName"
}
```

#### PlaylistRemoveResp (0x0403)
```json
{}
```

#### PlaylistContinueReq (0x0404)
```json
{}  // 房主请求继续播放列表中的下一首
```

#### PlaylistContinueResp (0x0405)
```json
{}
```

### 4.6 对战

#### BattleDataNotify (0x0500) — 客户端上报，即发即忘
```json
{
    "Score": 999000,          // uint: 当前分数
    "Accuracy": 99.50,        // float: 当前准确率
    "Perfects": 500,          // ushort: Perfect 数
    "Greats": 10,             // ushort: Great 数
    "Earlies": 3,             // ushort: Early 数
    "Lates": 2,               // ushort: Late 数
    "Misses": 1,              // ushort: Miss 数
    "FC": false,              // bool: 是否 Full Combo
    "Alive": true             // bool: 是否存活
}
```

#### BattleDataPush (0x0501) — 服务端广播给房间内的人
```json
{
    "Players": [
        {
            "Uid": "abc123",
            "Score": 999000,
            "Accuracy": 99.50,
            "Perfects": 500,
            "Greats": 10,
            "Earlies": 3,
            "Lates": 2,
            "Misses": 1,
            "FC": false,
            "Alive": true,
            "PingMS": 45
        }
    ]
}
```

#### BattleReturnedReq (0x0502)
```json
{}  // 玩家从游戏场景返回房间
```

#### BattleReturnedResp (0x0503)
```json
{}
```

### 4.7 聊天

#### ChatNotify (0x0600) — 客户端发送，即发即忘
```json
{
    "Message": "Hello everyone!"   // string: 聊天内容
}
```
> 服务端通过会话自动填充发送者的 UID 和昵称，客户端无需传。

#### ChatPush (0x0601) — 服务端广播
```json
{
    "AuthorUid": "abc123",         // string: 发送者 UID
    "AuthorName": "PlayerName",    // string: 发送者昵称 (系统消息为 "System")
    "Message": "Hello everyone!",  // string: 消息内容 (系统消息为消息 Key)
    "ExtraData": null,             // string?: 系统消息的附加数据
    "IsSystem": false              // bool: 是否为系统消息
}
```

### 4.8 社交

#### FriendRequestReq (0x0700)
```json
{
    "FriendUid": "def456"          // string: 目标玩家 UID
}
```
> 同一个接口用于：发送请求 / 接受请求 / 取消请求 / 删除好友。服务端根据当前双方的好友关系自动判断执行哪种操作。

#### FriendRequestResp (0x0701)
```json
{
    "Action": 1                    // int: 0=无操作, 1=已发送请求, 2=已接受, 3=已删除, 4=已取消
}
```

#### GetFriendsReq (0x0702)
```json
{}
```

#### GetFriendsResp (0x0703)
```json
{
    "Friends": [ /* PlayerInfo 对象数组，结构同 GetPlayerResp */ ]
}
```

#### FriendNotifyPush (0x0704)
```json
{
    "Action": "friend_request",    // string: friend_request / friend_accepted / friend_declined / friend_removed
    "PlayerUid": "def456",
    "PlayerName": "FriendName"
}
```

### 4.9 房间状态推送

#### LobbySyncPush (0x0800) — 服务端在房间状态变化时推送
```json
{
    "Id": 42,
    "Revision": 17,
    "Name": "My Lobby",
    "HostUid": "abc123",
    "PlayType": 0,
    "ChartSelection": 0,
    "Goal": 0,
    "MaxPlayers": 8,
    "PlaylistSize": 20,
    "IsPrivate": false,
    "Locked": false,
    "IsPlaying": false,
    "CurrentPlaylistEntry": 0,
    "Players": ["uid1", "uid2", "uid3"],
    "ReadyPlayers": ["uid1"],
    "Playlist": ["md5#diff#owner#name", "md5#diff#owner#name"],
    "PlayerDetails": [
        { "Uid": "uid1", "Name": "Player1", "PingMS": 30, "Status": 2 },
        { "Uid": "uid2", "Name": "Player2", "PingMS": 55, "Status": 2 }
    ]
}
```

客户端必须按房间 `Id` 记录已处理的最大 `Revision`，丢弃 `Revision` 小于等于已处理值的旧 `LobbySyncPush`。`Revision` 为 0 或缺失时按旧协议兼容处理。

#### LobbyAllReturnedPush (0x0801)
```json
{}  // 无 Payload，收到即表示所有人都已从游戏场景返回房间
```

#### LobbyKickedPush (0x0802)
```json
{
    "Reason": "被房主踢出"      // string: 被踢原因
}
```

---

## 五、连接生命周期

### 5.1 连接建立流程

```
客户端                                   服务端
  │                                        │
  │──── TCP Connect ──────────────────────→│ 接受连接，创建 ClientSession
  │                                        │
  │──── LoginReq {Uid, Name, IsReconnect}→│ 验证/创建玩家数据
  │                                        │ 绑定 Session ↔ Uid，替换旧连接
  │←─── LoginResp {Version, Token} ───────│ 返回版本和令牌
  │                                        │
  │     (连接建立完成，开始心跳循环)        │
  │                                        │
```

**登录约束**：
- TCP 建立后，客户端必须先发送 `LoginReq`，未登录连接只能处理 `Ping` 和 `LoginReq`。
- 服务端绑定 UID 时，如果该 UID 已有活跃 Session，必须关闭旧连接并以新连接为准。
- `Uid` 不能为空，`Name` 需要进行长度限制和非法字符清洗；清洗后的昵称才允许进入玩家档案和广播消息。
- `Token` 当前等同于连接会话令牌，仅用于标识本次连接。若未来用于鉴权，必须升级协议版本并明确过期时间和重放防护。

### 5.2 心跳保活

| 参数 | 值 | 说明 |
|------|------|------|
| 心跳间隔 | 15 秒 | 客户端每 15 秒发一次 Ping |
| 超时判定 | 45 秒 | 服务端超过 45 秒没收到任何消息，判定连接死亡 |
| RTT 计算 | 客户端记录 | 发 Ping 时记录时间戳，收到 Pong 后计算差值 |

- **客户端**：每 15 秒发送一次 `Ping`（OpCode=0x0001）
- **服务端**：收到 `Ping` 后立即回复 `Pong`（OpCode=0x0002），同时更新该连接的 `LastActivity`
- **注意**：服务端在收到**任何消息**时都会更新 `LastActivity`，不仅仅是 Ping
- 客户端发送 Ping 时记录本地时间戳，收到 Pong 后更新 `PingMS`。如果连续多次心跳发送失败或读取循环结束，统一进入断线流程
- `Pong` 是 Push 形态的系统消息，不带 `ReqId`，不参与 Request 超时字典

### 5.3 断线重连

#### 客户端策略：指数退避 + 全抖动 (Full Jitter)

当 TCP 连接意外断开后：

```
第1次重连: 等待 random(0, 1000ms)
第2次重连: 等待 random(0, 2000ms)
第3次重连: 等待 random(0, 4000ms)
第4次重连: 等待 random(0, 8000ms)
第5次重连: 等待 random(0, 16000ms)
...
最大等待上限: 30000ms
最大重试次数: 8 次
```

**计算公式**：
```
delay = random(0, min(BASE_DELAY * 2^attempt, MAX_DELAY))

其中:
  BASE_DELAY = 1000 ms
  MAX_DELAY  = 30000 ms
```

**全抖动的目的**：防止服务器宕机恢复后，所有客户端同时在同一时刻涌入重连，造成"雷鸣群效应 (Thundering Herd)"，把刚恢复的服务器再次打崩。

重连成功后，客户端发送 `LoginReq`（`IsReconnect = true`）。登录成功后由 Manager 层恢复玩家状态：重新获取玩家档案、房间状态和必要的 UI 状态。网络层只负责重新连通，不直接加入房间或操作 UI。

#### 服务端策略：宽限期 (Grace Period)

| 参数 | 值 |
|------|------|
| 宽限期时长 | 30 秒 |

- 当服务端检测到 TCP 连接断开后，**不立即清理该玩家的数据**
- 保留 30 秒的宽限期
- 宽限期内：
  - 该玩家在房间中的位置保留
  - 不广播"XX离开房间"的消息
  - 如果该 UID 的玩家重新连接并发送 `LoginReq(IsReconnect=true)`，则无缝恢复
- 宽限期结束后：
  - 将该玩家从所属房间移除
  - 广播离开消息
  - 如果房主离开，自动转移房主给房间内的下一个玩家
  - 清理空房间

**实现要求**：
- `SessionManager` 只能管理活跃连接；宽限期玩家需要单独的 `DisconnectedPlayer`/`PendingReconnect` 状态表或等价结构。
- `TcpServer.HandleDisconnect` 不得直接调用房间移除逻辑；它只标记断线时间并移除活跃 Session。
- 宽限期到期任务必须可取消。玩家在 30 秒内重连成功时，取消旧清理任务并恢复原房间状态。
- 宽限期玩家不应收到 Push；广播时跳过无活跃 Session 的 UID，但保留其房间席位。
- 如果当前实现尚未完成 Grace Period，文档验收时必须标为“未完成”，不能视为通过。

---

## 六、客户端架构

### 6.1 分层

```
┌───────────────────────────────────────────────┐
│              UI 层 (Panels / Displays)        │ ← 只负责展示和用户输入
│   订阅 Manager 层的事件，刷新界面              │
├───────────────────────────────────────────────┤
│              逻辑层 (Managers)                 │ ← 处理业务逻辑和状态缓存
│   LobbyManager / PlayerManager / BattleManager│
│   调用 NetworkClient 发送消息                  │
│   接收 PushDispatcher 的推送回调               │
├───────────────────────────────────────────────┤
│              网络层 (Network)                  │ ← 纯粹的网络通信
│   NetworkClient: 连接/断线/心跳/重连           │
│   PacketFramer: 4字节头拆包/粘包               │
│   PushDispatcher: 按 OpCode 派发推送           │
├───────────────────────────────────────────────┤
│              TCP Socket                       │
└───────────────────────────────────────────────┘
```

**严格禁止**：
- UI 层直接调用 NetworkClient（必须经过 Manager 层）
- Manager 层直接操作 TCP Socket
- 网络层包含任何业务逻辑

### 6.2 核心类职责

#### `NetworkClient`
```
职责: 管理 TCP 连接生命周期、心跳、重连、消息收发
暴露:
  - IsConnected: bool
  - PingMS: ushort
  - ConnectAsync(host, port): Task
  - Disconnect(): void
  - SendRequestAsync<TReq, TResp>(opCode, request, timeoutMs): Task<TResp>
  - SendNotifyAsync<T>(opCode, message): Task
  - OnPushReceived: event Action<ushort, JsonElement>
  - OnDisconnected: event Action
```

实现约束:
- `SendRequestAsync` 必须在发送前登记 `ReqId -> TaskCompletionSource`，收到 Response、超时、断线时都要移除 pending 项。
- `TaskCompletionSource` 建议使用 `RunContinuationsAsynchronously`，避免在接收循环线程内同步执行 Manager/UI 后续逻辑。
- `Disconnect()` 必须是幂等的。主动断开不应触发重连；意外断开才通知 `ReconnectionManager`。
- `ReceiveLoopAsync` 只负责拆包、反序列化、配对 Response、转交 Push，不写任何业务状态。
- 网络事件回调可能不在 Unity 主线程。Manager 或 UIManager 刷新 UI 前必须切回 Unity 主线程。

#### `PushDispatcher`
```
职责: 将收到的服务端 Push 消息，按 OpCode 派发给对应的 Manager 方法
注册方式:
  Register<T>(opCode, handler)
  
示例:
  Register<LobbySyncPush>(OpCodes.LobbySyncPush, LobbyManager.OnSyncReceived)
  Register<ChatPushMsg>(OpCodes.ChatPush, Chat.OnChatReceived)
  Register<BattleDataPush>(OpCodes.BattleDataPush, BattleManager.OnBattleDataReceived)
```

> 新增功能时，只需添加新的 OpCode、消息体类和注册一行 handler，无需改动框架。

实现约束:
- `PushDispatcher` 只做反序列化和路由，不直接修改 UI 或游戏对象。
- 同一个 OpCode 默认只能有一个业务 Owner。如果确实需要多个监听者，必须显式设计多播注册和注销机制，避免重复注册导致事件泄露。
- Manager 处理 Push 时要检查当前登录 UID、房间 ID 和状态版本，丢弃已过期的 Push。

---

## 七、服务端架构

### 7.1 技术选型

| 项目 | 选择 |
|------|------|
| 运行时 | .NET 8 (LTS) |
| 宿主 | Microsoft.Extensions.Hosting (Generic Host) |
| 依赖注入 | 内置 DI (IServiceCollection) |
| 日志 | 内置 ILogger |
| 数据存储 | players.json (IPlayerRepository 接口封装，未来可替换) |

### 7.2 分层

```
┌───────────────────────────────────────────────┐
│           数据持久化 (Data)                    │
│   IPlayerRepository → JsonPlayerRepository    │
├───────────────────────────────────────────────┤
│           业务逻辑 (Services)                  │
│   LobbyService: 房间增删改查、广播              │
│   PlayerService: 玩家数据管理                   │
│   BroadcastService: 向房间内玩家推送消息        │
├───────────────────────────────────────────────┤
│           消息处理 (Handlers)                   │
│   AuthHandler: 登录                            │
│   LobbyHandler: 房间操作                       │
│   BattleHandler: 对战数据转发                   │
│   ChatHandler: 聊天转发                        │
│   SocialHandler: 好友系统                      │
├───────────────────────────────────────────────┤
│           路由 (Routing)                       │
│   MessageRouter: 按 OpCode 派发到 Handler      │
├───────────────────────────────────────────────┤
│           网络 (Network)                       │
│   TcpServer: 监听端口、接受连接                 │
│   ClientSession: 单个连接的读写管理             │
│   SessionManager: 管理所有活跃的 Session        │
│   PacketFramer: 拆包/粘包                      │
├───────────────────────────────────────────────┤
│         .NET Generic Host                     │
└───────────────────────────────────────────────┘
```

### 7.3 核心类职责

#### `TcpServer` (实现 `BackgroundService`)
```
职责: 作为后台服务启动，监听 TCP 端口，接受新连接并为每个连接创建 ClientSession
```
约束:
- 未登录 Session 不得进入业务 Handler，除 `LoginReq` 和 `Ping` 外的消息都应拒绝。
- `HandleDisconnect` 只处理连接生命周期，不直接做复杂业务；断线宽限期恢复/清理由专门服务负责。
- 心跳超时主动关闭连接后，仍走同一套断线流程，避免超时断线和 Socket 异常断线行为不一致。

#### `ClientSession`
```
职责: 代表一个已连接的客户端
属性:
  - Uid: string (登录后赋值)
  - Token: string
  - LastActivity: DateTime
  - TcpClient: TcpClient
方法:
  - SendAsync(envelope): Task  // 线程安全的发送，内部使用 SemaphoreSlim
```
约束:
- `SendAsync` 必须串行化写入，同一连接不能并发写 `NetworkStream`。
- `Close`/`Dispose` 必须幂等，重复调用不得抛出异常。
- 发送失败应由调用方记录并触发断线处理，不得静默吞掉导致 Session 看似活跃。

#### `SessionManager`
```
职责: 管理所有活跃 Session 的字典 (ConcurrentDictionary<string, ClientSession>)
方法:
  - AddOrUpdateSession(uid, session)
  - RemoveSession(uid)
  - GetSession(uid): ClientSession?
  - GetAllSessions(): IEnumerable<ClientSession>
```
约束:
- `SessionManager` 只保存活跃连接，不保存宽限期断线玩家。
- 同 UID 新连接登录时，应替换旧 Session 并关闭旧连接。

#### `MessageRouter`
```
职责: 根据 OpCode 将消息派发到对应的 Handler 方法
注册方式:
  RegisterRequest<TReq, TResp>(opCode, handler)
  RegisterNotify<TReq>(opCode, handler)
```

#### `BroadcastService`
```
职责: 封装"向房间内所有人推送消息"的逻辑
方法:
  BroadcastToLobbyAsync(lobby, envelope, excludeUid?)
  BroadcastLobbySyncAsync(lobby)  // 推送完整的房间状态
```
约束:
- 广播前应先生成房间成员 UID 快照，再逐个查找活跃 Session。
- 没有活跃 Session 的 UID 可能处于宽限期，广播时跳过即可，不应因此从房间移除。
- 广播失败不能破坏房间锁；失败连接由断线流程统一清理。

### 7.4 线程安全策略

- **Session 写入**：每个 `ClientSession` 使用独立的 `SemaphoreSlim(1,1)` 保护 `NetworkStream.WriteAsync`，避免并发写入导致的字节流交叉
- **房间数据**：每个 `ServerLobby` 使用 `ReaderWriterLockSlim`，读多写少的场景下性能优于 `lock`
- **全局字典**：`SessionManager` 和 `LobbyService` 使用 `ConcurrentDictionary`
- **数据保存**：`IPlayerRepository` 的 `Save()` 方法内部使用 `lock` 保证文件写入的原子性

**锁使用规则**：
- 不得在持有 `ReaderWriterLockSlim` 的读锁或写锁时执行 `await`、网络发送、文件 IO 或可能回调业务层的代码。
- 需要广播时，先在锁内构造不可变快照或 DTO，退出锁后再调用 `BroadcastService`。
- 房间修改必须在写锁内完成；房间列表读取必须至少对单个房间加读锁或使用快照，避免枚举集合时被并发修改。
- 多个锁同时使用时，必须固定顺序：全局字典 -> 单个房间锁 -> 玩家数据锁。禁止反向拿锁，避免死锁。

---

## 八、关键业务流程

### 8.1 完整的游戏生命周期

```
连接 → 登录 → 浏览房间列表 → 创建/加入房间 → 选歌 → 准备 → 对战 → 结算 → 返回房间 → (循环)
```

### 8.2 选歌验证流程

当任何玩家向播放列表添加歌曲时，服务端必须执行以下验证：

1. **隐藏检查**：遍历房间内所有玩家的 `Hiddens` 列表，如果有人隐藏了该谱面 → 拒绝 (Result=2)
2. **自制谱面同步检查**：如果该谱面是自制谱面（chartId 长度 ≥ 16），遍历所有玩家的 `Customs` 列表，如果有人没有 → 拒绝 (Result=3)，并通过聊天广播告知缺少谱面的玩家名单
3. **重复检查**：如果播放列表中已存在相同的 chartId+difficulty → 拒绝
4. **容量检查**：如果播放列表已满 → 拒绝

### 8.3 准备与开始

- 任何玩家可以准备 (`LobbyReadyReq`)
- 当**所有玩家都准备完毕**时，服务端将 `IsPlaying` 设为 `true`，并通过 `LobbySyncPush` 通知所有人
- 客户端收到 `IsPlaying=true` 后，各自本地进入游戏场景

### 8.4 对战数据同步

- 游戏进行中，每个客户端按可配置的间隔（默认 500ms）发送 `BattleDataNotify`
- 服务端收到后，汇总房间内所有人的最新数据，通过 `BattleDataPush` 广播给房间内所有人（包括发送者本人，以便客户端获取其他人的数据）
- **注意**：客户端上报时不需要携带 UID，服务端通过 Session 自动识别

### 8.5 对战结束与结算

- 当某个玩家的游戏结束（歌曲打完或死亡），客户端仍然保持发送 `BattleDataNotify`（`Alive=false`），直到本地返回房间场景
- 返回房间场景后，客户端发送 `BattleReturnedReq`
- 当**所有人**都发送了 `BattleReturnedReq`，服务端广播 `LobbyAllReturnedPush`
- 客户端收到后，清理对战 UI，显示结算界面

### 8.6 房间密码验证

- 创建房间时可设置 `Password`（非空即为私密房间）
- 加入私密房间时，客户端必须在 `JoinLobbyReq` 中携带 `Password` 字段
- 服务端对比密码，不匹配则返回 `Success=false, Reason="密码错误"`
- 房间列表中，私密房间**不出现**在公开列表（`GetLobbiesResp`）中

---

## 九、配置参数一览

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 服务端监听端口 | 10423 | TCP 端口 |
| 最大帧大小 | 1 MB | 超过则断开连接 |
| 心跳间隔 | 15 秒 | 客户端发 Ping 的频率 |
| 心跳超时 | 45 秒 | 服务端判定连接死亡 |
| 重连最大次数 | 8 次 | 客户端尝试重连的上限 |
| 重连基础延迟 | 1000 ms | 指数退避的基数 |
| 重连最大延迟 | 30000 ms | 指数退避的上限 |
| 断线宽限期 | 30 秒 | 服务端保留玩家数据的时长 |
| Request 超时 | 15 秒 | 客户端等待 Response 的上限 |
| 对战同步间隔 | 500 ms | 默认值，可通过设置调整 (200ms~2000ms) |
| 房间列表刷新间隔 | 3000 ms | 仅用于主动刷新大厅列表；房间内部状态以 Push 为准 |
| 最大房间人数 | 10 | |
| 最小房间人数 | 2 | |
| 播放列表最大容量 | 32 | |
| 聊天消息最大长度 | 256 字符 | |

---

## 十、共享协议库目录结构

```
MDEN.Protocol/
├── MDEN.Protocol.csproj          // net6.0，双端都引用同一项目
├── OpCodes.cs                    // 所有操作码常量定义
├── Envelopes/
│   ├── ClientEnvelope.cs         // 客户端 → 服务端的消息信封
│   └── ServerEnvelope.cs         // 服务端 → 客户端的消息信封
├── Messages/
│   ├── Auth/
│   │   └── AuthMessages.cs
│   ├── Player/
│   │   └── PlayerMessages.cs
│   ├── Lobby/
│   │   ├── LobbyMessages.cs
│   │   └── EmptyLobbyMessages.cs
│   ├── Playlist/
│   │   └── PlaylistMessages.cs
│   ├── Battle/
│   │   ├── BattleMessages.cs
│   │   └── EmptyBattleMessages.cs
│   ├── Chat/
│   │   └── ChatMessages.cs
│   └── Social/
│       └── SocialMessages.cs
├── Enums/
│   └── ProtocolEnums.cs          // PlayerStatus / LobbyGoal / LobbyPlayType / LobbyChartSelection
└── Models/
    └── SharedModels.cs           // PlayerSyncEntry / BattlePlayerEntry / LobbyListEntry
```

---

## 十一、当前实现必须重点验收的风险点

以下项目不是新协议，而是根据当前客户端与 `D:\MuseDashStuff\MDEN.Server` 代码检查出的落地风险。后续验收时应逐项确认。

1. **Grace Period 尚未完整落地**：服务端 `TcpServer.HandleDisconnect` 当前只移除活跃 Session，`AuthHandler` 的重连恢复仍需实现。未完成前，断线重连不能标为通过。
2. **锁内 await 风险**：房间同步需要先在房间锁内构造快照，退出锁后再广播。不得在持有 `ReaderWriterLockSlim` 时执行 `await Broadcast...`。
3. **客户端主动断开与异常断开要区分**：主动 `Disconnect()` 不应触发自动重连；读取循环异常才进入 `ReconnectionManager`。
4. **UI 更新线程要隔离**：`PushDispatcher` 和 `NetworkClient` 的回调不保证在 Unity 主线程，Manager 通知 UI 前必须切主线程。
5. **协议库版本要单源**：服务端和客户端必须引用同一个 `MDEN.Protocol` 项目，不允许复制 DLL 后各自修改消息类。

---

> **文档版本**: v1.1
> **最后更新**: 2026-06-13
> **适用范围**: MDEN 客户端 Mod 和 MDEN.Server 服务端的全部网络相关代码
