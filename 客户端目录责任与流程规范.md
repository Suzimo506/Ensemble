# MDEN 客户端代码目录责任与流程定位表

基于 `MDEN` (Mod客户端) 目前的网络架构实现，整理出以下目录责任划分与流程规范，以保证业务逻辑、网络通信和 UI 生命周期解耦。

## 目录责任表

| 目录/模块 | 来源类型 | 主要职责 | 以后应该放什么 | 禁止放什么 | 对应的机制 | 实例文件 |
|---|---|---|---|---|---|---|
| `Bootstrap/` | 项目自定义 | 串联 MelonLoader 生命周期、场景切换、每帧更新与关闭流程。 | 新增全局初始化顺序、场景入口编排、退出清理编排。 | 具体业务规则、网络协议处理、UI 细节绘制。 | MelonLoader 生命周期回调 | `ClientBootstrapper.cs` |
| `Network/` | 项目自定义 | 处理所有纯粹的底层网络通信（封包、Socket、心跳、断线重连）。 | 网络安全策略加密、多节点分发逻辑。 | 任何与游戏引擎（Unity）直接相关的组件调用或具体业务逻辑（如操作UI或角色状态）。 | 原生 TCP 与 Task 异步流 | `NetworkClient.cs` |
| `Network/PushDispatcher`| 项目自定义 | 将网络层收到并解析好的推送事件，按 `OpCode` 路由转发给业务层。 | 注册新的推送数据包模型。 | 业务逻辑处理本身。 | 委托 Action 路由 | `PushDispatcher.cs` |
| `Network/Reconnection` | 项目自定义 | 全抖动指数退避重连算法的具体实现。 | 未来如果是多 IP 测速，可以将测速重连写在这里。 | 连接成功后立刻帮玩家加入某个游戏内的房间（越权调用）。 | Task.Delay 与 Random | `ReconnectionManager.cs` |
| `Threading/` | 项目自定义 | 维护网络/后台线程进入 Unity 主线程的边界适配。 | 主线程派发抽象、后台回调进入 UI 前的统一入口。 | 具体 UI 组件、业务状态机、Socket 读写。 | Action 队列适配 | `ClientThreadDispatcher.cs` |
| `Managers/` | 项目自定义 | 游戏内的具体业务管理器（负责组装网络和UI交互）。 | `LobbyManager.cs`, `PlayerManager.cs` 等，订阅 `PushDispatcher`。 | 底层 `TcpClient` 或序列化操作。 | 静态单例或依赖注入 | `LobbyManager.cs` |
| `UI/` | 项目自定义 | 纯粹的界面展示逻辑。 | Unity 组件、界面刷新代码。 | 发送网络请求（必须经过 Manager 层中转）。 | MelonLoader 注入 | `UI/Core/RoomHudController.cs` |

---

## 系统流程与架构定位说明

### 1. 请求入口在哪里？
* **主动发起**：由 UI 层点击按钮等触发 -> 调用 `Manager` 层的方法 -> `Manager` 层构建数据模型，并调用 `NetworkClient.Instance.SendRequestAsync` 发起网络请求。
* **被动接收**：`NetworkClient.ReceiveLoopAsync` 一直在后台运行。当收到包时，如果是对请求的响应，则解开对应的 `TaskCompletionSource`（类似 async/await 立即往下执行）；如果是服务端的 Push 推送，则抛给 `PushDispatcher.Dispatch`。
* **主线程切换**：网络层不直接调用 `UI/Core/MainThreadDispatcher`，而是通过 `Threading/ClientThreadDispatcher` 进入主线程，避免底层网络代码依赖 UI 目录。

### 2. 业务规则在哪里？
* **严格剥离**：网络底层代码（如封包、断线重试）都在 `Network/` 里。
* 真正的游戏业务规则，比如“如何处理收到队友的分数变动”、“如何刷新聊天窗口”，都必须写在 `Managers/` 里。Manager 在游戏启动时，通过 `PushDispatcher.Instance.Register` 来监听对应的消息。

### 3. 错误处理在哪里？
* **断线与异常**：网络突然断开、流异常，统一由 `NetworkClient.cs` 中的 `ReceiveLoopAsync` 的 try-catch 捕获，然后抛出 `OnDisconnected` 事件，触发 `ReconnectionManager` 的退避重连机制。
* **请求异常**：如果请求超时（超过 15 秒）或者服务端显式返回错误，`SendRequestAsync` 内部会直接抛出异常，业务层的 `Manager` 在 `await` 时需要加上 `try-catch` 进行界面弹窗提示。
