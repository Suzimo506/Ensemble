# MDME 客户端代码规范约束

本文档是 MDME 客户端 Mod 的强制代码规范。所有 AI 和开发者在新增功能、修改 Bug、重构代码、撰写验收报告时都必须遵守本文档。

---

## 一、开发前必须确认的架构边界

开发任何新功能前，必须先回答并落实以下问题：

1. 这个功能属于 `Network/`、`Managers/`、`UI/` 还是共享协议库 `MDME.Protocol/`。
2. 是否能复用现有 `NetworkClient`、`PushDispatcher`、`ReconnectionManager`、`UIManager`、ViewModel 或已有 Manager。
3. 是否需要新增 OpCode 或消息体；如果需要，必须先改 `MDME.Protocol`，再分别接入客户端和服务端。
4. 是否会引入新的 UI 状态、异步请求、Push 订阅或事件监听；如果会，必须设计生命周期清理和过期结果丢弃。
5. 是否会影响旧功能；如果会，必须列出受影响的旧流程并纳入验收。

禁止为了单个功能绕过现有框架。能通过扩展 Manager、注册 Push、增加 ViewModel 字段解决的问题，不允许直接在 UI 或 Network 层硬写特殊逻辑。

---

## 二、目录职责与解耦规则

### 1. `Network/`

- 只负责 TCP 连接、封包拆包、JSON 序列化、Request/Response 配对、心跳、断线通知和重连调度。
- 不得引用 Unity UI、popuplib、游戏对象、房间业务、玩家状态业务。
- 不得在 `ReceiveLoopAsync` 中写大厅、聊天、对战、UI 刷新等业务判断。
- `NetworkClient` 的事件回调不保证在 Unity 主线程，任何 UI 更新必须由上层切回主线程。

### 2. `Managers/`

- 负责业务状态缓存、请求组装、调用 `NetworkClient`、订阅 `PushDispatcher`、生成 UI 可读的 ViewModel。
- 业务判断写在 Manager，例如是否已登录、是否在房间、当前玩家是否房主、是否可点击某按钮。
- Manager 不直接操作具体按钮、文本、图片、窗口实例。
- Manager 不直接操作 `TcpClient`、`NetworkStream`、JSON 字符串或底层 Envelope。

### 3. `UI/`

- 只负责创建界面、展示 ViewModel、绑定用户输入、向 Manager 发起调用。
- UI 可以根据 ViewModel 字段显示或隐藏控件，但不得自己读取网络状态或跨层计算业务规则。
- UI 不得直接调用 `NetworkClient`。
- 所有窗口打开、关闭、遮罩、错误提示必须经过 `UIManager` 或其下属 UI Flow。
- 所有事件监听必须登记到窗口生命周期中，关闭时逐个解绑。

### 4. `MDME.Protocol/`

- 只存放双端共享的 OpCode、Envelope、Request、Response、Push、Notify、枚举和共享 DTO。
- 禁止客户端单独创建与服务端重复但结构不同的消息类。
- 消息字段变更必须同步考虑客户端、服务端、文档和兼容性。

---

## 三、新功能开发规则

### 1. 必须复用旧框架

新增功能优先使用以下扩展方式：

- 新 Request：新增协议模型 -> Manager 调用 `SendRequestAsync` -> 服务端 Handler/Service 响应。
- 新 Push：新增协议模型 -> `PushDispatcher.Register<T>` -> Manager 更新状态 -> UIManager 刷新 UI。
- 新 UI：新增窗口或组件 -> 使用已有 UI 基类、锁机制、事件清理机制、ViewModel。
- 新状态：优先加 Manager 状态和 ViewModel 字段，不让 UI 自己到处查全局状态。

只有当现有框架无法表达新需求时，才允许扩展底层框架。扩展底层框架必须说明为什么旧框架不够用，以及如何避免影响旧功能。

### 2. 必须方便未来修改

新增代码必须满足：

- 单个类职责清晰，不能把 UI、业务、网络混在一个方法里。
- 新增业务入口必须有明确方法名，例如 `JoinLobbyAsync`、`SetReadyAsync`、`SendChatAsync`。
- 参数和返回值使用强类型模型，不使用散乱字符串或魔法数组传递业务数据。
- 不复制粘贴已有逻辑；公共逻辑下沉到 Manager 或工具方法。
- 不把临时规则硬编码在 UI 文案或按钮回调里。

### 3. 异步与 UI 防错规则

- 会发网络请求的 UI 操作必须在发请求前加 UI 锁，并在 `finally` 中解锁。
- UI 锁必须使用 token/handle，不能随意全局解锁。
- Response/Push 回来后必须检查当前窗口版本、房间 ID 或状态版本，过期结果必须丢弃。
- 不得让旧请求结果覆盖新窗口状态。
- Push 引发的刷新必须切回 Unity 主线程。

### 4. 富文本颜色格式

- `UI/Windows/` 下的 `PopupLib` 窗口文本必须写成 `<color=fff700ff>文本</color>`。
- `UI/Core/`、`UI/Displays/` 等游戏内原生 `UnityEngine.UI.Text` 文本必须写成 `<color=#fff700ff>文本</color>`。
- 禁止跨 UI 体系一刀切替换颜色格式；修改富文本前必须确认目标文本属于 `PopupLib` Window 还是游戏原生 UI。
- 玩家输入或服务端下发的十六进制颜色在渲染前必须去掉前导 `#`，校验为 6 位或 8 位十六进制，再由对应 UI 体系决定是否添加前导 `#`。

### 5. 多语言文本规则

- 所有玩家可见文本必须通过 `Managers/I18nManager.cs` 的 `T` 或 `Tf` 获取，不得在 UI、Manager、Patch 中直接硬编码中文、英文、日文、韩文提示。
- 新增 UI 文案必须同时提供简体中文、英文、繁体中文、日语、韩语；语言选择跟随游戏内语言设置，不新增独立语言配置。
- 服务端返回的固定业务原因只允许作为协议匹配或兜底日志使用；显示给玩家前必须映射到 i18n key。
- 富文本颜色参数只作为 `Tf` 参数传入，文案本身不得拼接玩家输入；玩家输入进入富文本前必须先做转义或格式校验。
- 修改已有功能时，如果新增按钮、弹窗、横幅、排行榜、房间描述、Toast 或锁屏文案，必须同步补齐 i18n 字典和对应失败提示。

---

## 四、修改代码的验收规则

每次开发完成后，必须从以下层面验收。

### 1. 架构验收

必须证明：

- UI 没有直接调用 `NetworkClient`。
- Network 层没有业务逻辑和 UI 逻辑。
- Manager 是 UI 与 Network 的唯一业务中转。
- Push 注册点清晰，没有重复注册和无法注销的问题。
- 新消息模型位于 `MDME.Protocol`，没有客户端私有重复 DTO。

### 2. 行为验收

必须验证成功路径和失败路径：

- 成功路径：例如创建房间成功、加入房间成功、发送聊天成功。
- 业务失败：例如房间不存在、密码错误、房间已满、未登录。
- 网络失败：例如超时、断线、重连中。
- 连点和乱序：连续点击按钮不会重复提交，旧 Response 不会覆盖新 UI。
- Push 刷新：多人状态变化时 UI 能正确刷新且不闪烁、不丢输入状态。

### 3. 构建与静态检查

如果项目当前能构建，必须运行对应构建命令。若不能构建，必须说明原因。

验收至少包含：

- 编译是否通过。
- 修改文件是否符合目录职责。
- 是否新增未清理事件监听。
- 是否新增未处理的异步异常。
- 是否新增 `TaskCompletionSource` 但未使用 `RunContinuationsAsynchronously`。
- 是否新增 JSON 序列化但未使用统一协议配置。

### 4. 回归范围

修改底层网络、协议、Manager、UIManager 时，必须额外回归：

- 登录连接。
- 心跳与断线。
- Request 超时。
- Push 派发。
- 房间列表。
- 创建/加入/离开房间。
- 聊天或对战数据同步中至少一个 Push 场景。

---

## 五、开发完成后的汇报规范

向用户汇报时必须包含以下内容。

### 1. 功能边界

必须明确说明：

- 本次做了什么。
- 没有做什么。
- 哪些文件属于 UI，哪些属于 Manager，哪些属于 Network，哪些属于 Protocol。
- 本次是否影响服务端；如果影响，必须说明双端协议是否同步。

### 2. 调用链示例

必须举至少一个真实调用链例子。格式如下：

```text
玩家点击加入房间
-> UI/LobbyWindows/LobbiesWindow.cs 申请 UI 锁并调用 LobbyManager
-> Managers/LobbyManager.cs 组装 JoinLobbyRequest
-> Network/NetworkClient.cs 发送带 ReqId 的 Request
-> 服务端返回 JoinLobbyResponse
-> LobbyManager 更新 CurrentLobby/ViewModel
-> UIManager 切主线程打开 RoomWindow 并释放 UI 锁
```

### 3. 证据要求

汇报必须拿出证据，不能只说“已完成”。

证据至少包含：

- 修改文件清单。
- 关键代码位置，例如 `NetworkClient.cs`、`LobbyManager.cs`、`LobbiesWindow.cs`。
- 构建结果或无法构建的明确原因。
- 成功路径的验证结果。
- 至少一个失败路径的验证结果。
- 如果涉及序列化，给出 null 字段省略或 ReqId 配对的证据。
- 如果涉及 UI，说明事件清理、UI 锁、过期响应丢弃如何验证。

推荐汇报模板：

```text
本次完成：
- ...

边界说明：
- UI：...
- Manager：...
- Network：...
- Protocol：...

验证：
- 构建：通过 / 未运行，原因是 ...
- 成功路径：...
- 失败路径：...
- 回归影响：...

证据：
- 文件 A 中 ...
- 文件 B 中 ...
```

---

## 六、注释规范

- 禁止新增 XML 文档注释，尤其禁止新增 `<summary>`。
- 注释使用简短中文，解释“为什么这样做”，不要复述代码“做了什么”。
- 能通过清晰命名表达的，不写注释。
- 不写空泛注释，例如“处理数据”“执行逻辑”“调用方法”。
- 不保留过期 TODO。必须写 TODO 时，需要说明触发条件或后续动作，例如 `// TODO: Grace Period 实现后恢复房间状态`。

示例：

```csharp
// 防止旧窗口的异步结果覆盖当前窗口
if (generation != _currentGeneration) return;
```

禁止使用 XML 文档注释形式给普通方法补说明，尤其不要为了重复方法名而添加文档注释。

---

## 七、禁止事项

- 禁止 UI 直接访问 `NetworkClient`。
- 禁止 Network 层引用 UI、Manager、游戏业务对象。
- 禁止在 UI 点击回调里拼 JSON、拼 Envelope 或直接写 OpCode。
- 禁止新增无法解绑的事件监听。
- 禁止异步请求没有超时、没有异常处理、没有 finally 解锁。
- 禁止吞异常后不汇报给 Manager 或 UI。
- 禁止复制协议类到客户端私有目录。
- 禁止用魔法数字表达 OpCode、玩家状态、房间状态；必须使用 `OpCodes` 和协议枚举。
- 禁止为了临时功能绕过 UIManager、PushDispatcher、Manager 层。

---

## 八、AI 执行要求

AI 修改客户端代码时必须遵守：

1. 先读相关规范和现有代码，再改代码。
2. 优先复用旧框架，不擅自新建平行体系。
3. 修改前说明将改哪些层，修改后按本文档汇报。
4. 如果发现规范与代码冲突，必须指出冲突并给出选择理由。
5. 如果无法验收，必须明确说“未验收”，并说明缺少什么条件。

---

> **文档版本**: v1.0  
> **最后更新**: 2026-06-13  
> **适用范围**: MDME 客户端 Mod 的全部代码与所有 AI/开发者提交
